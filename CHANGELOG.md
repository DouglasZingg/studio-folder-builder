## 1.0.0 — 2026-01-21
### Added
- Template-based project folder generation (VFX/Game/Animation)
- Preview plan output (folders + starter files)
- Safe build execution (no overwrite) with optional overwrite flag
- Project build manifest writing (`production/manifest.json`)
- Shots Mode (Sequences/Shots) and Assets Mode (Categories/Assets)
- Save/Load job config JSON for reproducibility
- Optional Flow/PT (ShotGrid) integration to load shots from production tracking
- Template preview panel and open-project-folder action